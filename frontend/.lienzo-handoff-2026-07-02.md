# Lienzo — Handoff KnowTwin Frontend · 2026-07-02

## Quién eres
Lienzo, director artístico y lead de diseño de Eco Consulting. Estás ejecutando el workflow-frontend para KnowTwin. Pepe (tu jefe) te dio carta blanca. Estás en caveman ultra mode.

## Proyecto
- **Producto:** KnowTwin — plataforma de captura de conocimiento tácito para offboarding empresarial. Fork de EcoDB.
- **Repo frontend:** `C:\Users\Admin\Documents\KnowTwin\frontend\`
- **Backend API:** `http://localhost:8090` (FastAPI, 188+ tests, Phase 2 completo)
- **EcoDB dashboard (REFERENCIA):** `C:\Users\Admin\Documents\EcoDB\dashboard\src\`
- **DESIGN.md:** `C:\Users\Admin\Documents\KnowTwin\DESIGN.md` (autoridad visual, hereda de EcoDB)
- **Spec:** `F:\obsidian\GuildWars\Eco_Consulting\Faro\Informes\Diseño\KnowTwin\deep_design_v3_spec.md`
- **Handoff backend:** `C:\Users\Admin\Documents\KnowTwin\FRONTEND_HANDOFF.md`
- **Sesión Faro:** `F:\obsidian\GuildWars\Eco_Consulting\Faro\Sesiones\2026-07-02_knowtwin_frontend\`
- **Backlog deuda:** `F:\obsidian\...\2026-07-02_knowtwin_frontend\backlog_deuda.md`

## Stack
React 18 + Vite 6 + Tailwind 3.4 + TanStack Query + react-router 6 + **Electron 33** (full desktop app como EcoDB). Main process: IPC bridge (window.knowtwin), secure-store (safeStorage/DPAPI), WS proxy por main, CSP, SSRF guard.

## Keys de dev
- Admin (all endpoints): `<YOUR_API_KEY>` (user 1, is_super — generate with bootstrap_first_apikey.py)
- Employee (Juan Garcia, user 562): `<YOUR_EMPLOYEE_KEY>` (generate via POST /auth/api-keys)
- Demo seed: Juan Garcia, 34 claims, 8 docs, 16 disputed, Banco Norte 46.3%

## Contratos REALES confirmados (Spec tiene paths MAL)
- Claims: `GET /claims?project_id=1` → {items, total} (NO /projects/{id}/claims)
- Upload: `POST /documents/upload?project_id=&visibility=&trust_hint=` (query, path plano, campo 'file')
- Interview REST: prefix `/interviews/{id}/...`
- WS: `/projects/{pid}/interviews/{sid}/ws?key=` (montado aparte)
- Stats: `/api/v1/stats/knowledge|system|graph|search|timeline|agents|memories` (todos 200)
- Attention inbox: `/admin/attention-inbox/summary` + `/details?decision_class=` (6 clases incl pending_disputes + pending_deletions)
- Graph: `/graph/all`, `/graph/subgraph`, `/graph/neighbors`, `/graph/search`, `/graph/clusters`, `/graph/stats`
- Ontology: `/admin/entity-dictionary` CRUD, `/admin/stop-entities`, `/admin/alias-candidates` + scan, `/admin/merge-entities` + undo, `/admin/predicates` CRUD, `/admin/graph-vocabulary`

## Equipo relay
- **Mi equipo frontend:** frontend-builder (Opus), adversarial-visual, adv-code-2, adv-seg-2, verificador-2
- **Equipo de Hilo (backend, NO tocar):** adv-code, adv-seg, verificador, code
- **Hilo:** supervisor backend, soporte para endpoints/fixes
- **Sala relay:** front-knowtwin

## Estado — QUÉ ESTÁ HECHO (y aprobado)
1. ✅ Foundation visual (tokens, tailwind, index.css glass base, DM Mono/Hanken)
2. ✅ Chasis Electron (main/preload/secure-store/config-store, CSP, SSRF guard, WS proxy por main) — HARDENED por adv-seg-2 (VS1 redirect:manual, VS2 https no-loopback, VS3 permissionCheck, VS4 size guard, VS5 base-uri/form-action)
3. ✅ Rewire data layer (api.ts→bridge, auth.ts→bridge, ws.ts→main proxy, getApiKey ELIMINADO)
4. ✅ Kit componentes glass (GlassCard, Panel, Button terracota, Toggle, ThemeToggle, SearchField, StatCard, BrandMark, Dot, Chip, Badge base + 5 badges dominio §7, Toasts, ConfirmDialog) — C1 StatCard FIXED (valor ink-1 + dot)
5. ✅ App shell (NavRail + AppBar + StatusPill /health + ThemeToggle + theme-init.js anti-FOUC)
6. ✅ Curation Inbox (batch approve, force approve, ClaimRow glass, SegmentedControl filter, audit icon, badges dot+ink, Toasts) — useClaims fixed para {items,total}
7. ✅ Dispute resolution (DisputeQueue side-by-side, doc_strength breakdown, ResolveDialog, members dropdown)
8. ✅ Deletion workflow (employee request + curator review, GDPR ConfirmDialog irreversible, SettingsDrawer glass retrofit, RetentionPolicy wired)
9. ✅ Audit trail drawer (glass drawer, timeline ASC, JSON details SafeText as="pre")
10. ✅ Score chip (graphite mono, tooltip process framing §7.6, no "ranking")
11. ✅ Twin disputes enhanced (DisputePanel doc_strength, SourcePanel, CoverageOverview, TwinChat glass)
12. ✅ Document ingestion UX (upload via bridge, trust_hint, curator/run síncrono, docs polling)
13. ✅ Dashboard Command Center (stats reales, attention inbox 6 clases, activity feed, coverage overview, ingestion snapshot — todos endpoints 200 tras fix de Hilo)
14. ✅ Graph view (ForceGraph2D, dark screen, node colors coverage, inspection panel, search fuzzy, coverage legend — PERO simplón vs EcoDB)
15. ✅ Legacy blue sweep completo (grep 0 legacy classes)

## Estado — QUÉ FALTA (y por qué)
**Pepe rechazó las vistas #13/#14/#15 como "no equivalentes a EcoDB"** tras comparar screenshots lado a lado. Tiene razón. Las versiones actuales son funcionales pero NO portan la riqueza del dashboard EcoDB. La diferencia es abismal en:

- **Command Center:** falta Knowledge Health (6 stats + top by degree), CTAs en attention inbox, subtítulos en stats, nav groups (WORKSPACE/GOVERNANCE), system monitor bar
- **Graph Studio:** falta edges/connections, nodos por TIPO (no solo coverage), TunePanel sliders, hop selector, context menu, merge confirm, box-select, type legend con counts, status bar
- **Ingestion:** es un tab, no una vista dedicada. Falta metric tiles, live activity, document detail panel con chunks/reindex/delete
- **Knowledge Explorer:** NO EXISTE en KnowTwin. EcoDB tiene browse claims con search+filters+advanced, MemoryRow, DocRow, DocPreviewModal
- **Decisions Inbox:** NO EXISTE. EcoDB tiene split view con 4 class tabs, resolve/defer/dismiss, WhyBox
- **Ontology Console:** pendiente (builder estaba trabajando en #16)
- **Shell:** falta CommandPalette (Cmd+K), MemoryDrawer, SystemMonitor (status bar), nav groups WORKSPACE/GOVERNANCE, user profile

## Lección aprendida (CRÍTICA)
**NO inventar, NO aproximar. PORTAR del código fuente real de EcoDB.** El builder DEBE leer cada archivo de EcoDB/src/pages/ ANTES de portar su equivalente KnowTwin. La iteración anterior falló porque di briefs basados en el design.md (abstracto) en vez del código real (concreto). "Equivalente" significa que si pones las dos apps lado a lado, se ven como el mismo producto con datos distintos.

## Archivos EcoDB a portar (leídos, entendidos)
- `src/pages/CommandCenter.tsx` (340 líneas) — 4 stats + AttentionInbox + ActivityFeed + KnowledgeHealth + Ingestion
- `src/pages/GraphStudio.tsx` (632 líneas) — ForceGraph2D completo con tune/hop/merge/box-select/inspector/legend
- `src/pages/Ingestion.tsx` (403 líneas) — vista dedicada con metric tiles + live + doc list + detail panel + upload
- `src/pages/KnowledgeExplorer.tsx` (481 líneas) — GAMR search + filters + advanced + MemoryRow + DocRow
- `src/pages/DecisionsInbox.tsx` (362 líneas) — 4 class tabs + split view + resolve/defer/dismiss
- `src/pages/OntologyConsole.tsx` (1115 líneas) — 4 tabs (entities/predicates/aliases/dictionary) con merge flow
- `src/components/AppShell.tsx` (122 líneas) — shell con ErrorBoundary + CommandPalette + SystemMonitor + overlays
- Graph support: `src/components/graph/TunePanel.tsx`, `ContextMenu.tsx`, `MergeConfirmModal.tsx`, `drawNode.ts`, `graphTypes.ts`

## Prompt para re-lanzar el workflow
El prompt completo está en el mensaje anterior a este handoff — Pepe lo copia y lanza `/workflow-frontend` con él. Resume: portar las 7 vistas EcoDB literalmente, adaptando endpoints a KnowTwin, leyendo el código fuente real, no aproximando.

## Tareas pendientes
- #16 Ontology Console (in_progress, builder estaba en ella)
- #15 Graph Studio REWRITE (rechazado por Pepe)
- #17 Command Center REWRITE (rechazado)
- #14 Ingestion REWRITE (rechazado)
- NUEVO: Knowledge Explorer (no existía)
- NUEVO: Decisions Inbox (no existía)
- NUEVO: Shell upgrade (CommandPalette, SystemMonitor, nav groups, user profile)
- #12 Bug-hunt (pendiente hasta que las vistas estén)
- #13 Empaquetar + instalar (final)

## Seguridad (invariantes NON-NEGOTIABLE)
- Zero dangerouslySetInnerHTML. Todo texto externo vía SafeText.
- API key solo en main process (secure-store, nunca al renderer).
- CSP: redirect:manual en todos los fetch, setPermissionCheckHandler, base-uri 'self', form-action 'self'.
- Frontend NO es boundary de seguridad.
